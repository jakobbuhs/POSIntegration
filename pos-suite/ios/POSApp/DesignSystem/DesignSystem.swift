import SwiftUI

enum DesignSystem {
  enum Spacing {
    static let xs: CGFloat = 8
    static let s: CGFloat = 12
    static let m: CGFloat = 16
    static let l: CGFloat = 24
  }

  enum CornerRadius {
    static let standard: CGFloat = 16
  }

  enum Sizing {
    static let minTapHeight: CGFloat = 56
    static let horizontalPadding: CGFloat = 20
  }
}
